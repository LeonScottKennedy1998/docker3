{
  "name": "mpt-store-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:with-mobile": "concurrently -n web,api,expo \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:mobile\"",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:mobile": "cd mobile && npx expo start --lan",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../mobile && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node index.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "react-pdf": "^10.3.0"
  }
}
